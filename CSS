body {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    background-color: #1a1a1a;
    color: #ffffff;
}

.container {
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
}

header {
    text-align: center;
    margin-bottom: 20px;
}

header h1 {
    font-size: 36px;
}

header p {
    font-size: 18px;
}

main {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
}

section {
    text-align: center;
    max-width: 300px;
    margin: 20px auto; /* Alterado para centralizar verticalmente */
}

section img {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
}

section h2 {
    margin-top: 10px; /* Adicionado um pequeno espaçamento acima do título */
}
